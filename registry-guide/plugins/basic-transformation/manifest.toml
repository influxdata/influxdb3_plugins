manifest_schema_version = "1.1"

[plugin]
name = "basic-transformation"
version = "0.1.0"
description = "Apply basic transformations to incoming data."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"
