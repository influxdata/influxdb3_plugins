manifest_schema_version = "1.1"

[plugin]
name = "kafka-subscriber"
version = "0.1.0"
description = "Subscribe to Kafka topics and write messages to InfluxDB."
triggers = ["process_scheduled_call"]

[dependencies]
database_version = ">=3.0.0"
