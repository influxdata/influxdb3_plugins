manifest_schema_version = "1.1"

[plugin]
name = "amqp-subscriber"
version = "0.1.0"
description = "Subscribe to AMQP queues and write messages to InfluxDB."
triggers = ["process_scheduled_call"]

[dependencies]
database_version = ">=3.0.0"
