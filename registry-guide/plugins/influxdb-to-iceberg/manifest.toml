manifest_schema_version = "1.1"

[plugin]
name = "influxdb-to-iceberg"
version = "0.1.0"
description = "Export InfluxDB data to Apache Iceberg tables."
triggers = ["process_scheduled_call"]

[dependencies]
database_version = ">=3.0.0"
