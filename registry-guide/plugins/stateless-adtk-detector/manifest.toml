manifest_schema_version = "1.1"

[plugin]
name = "stateless-adtk-detector"
version = "0.1.0"
description = "ADTK-based anomaly detection."
triggers = ["process_scheduled_call"]

[dependencies]
database_version = ">=3.0.0"
