manifest_schema_version = "1.1"

[plugin]
name = "downsampler"
version = "0.2.0"
description = "A new write-processing plugin."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"
