manifest_schema_version = "1.1"

[plugin]
name = "notifier"
version = "0.2.0"
description = "Send notifications via HTTP, Slack, or Discord."
triggers = ["process_request"]

[dependencies]
database_version = ">=3.0.0"
