manifest_schema_version = "1.1"

[plugin]
name = "mad-check"
version = "0.1.0"
description = "Median absolute deviation anomaly detection."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"
