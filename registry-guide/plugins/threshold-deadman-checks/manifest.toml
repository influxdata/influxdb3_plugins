manifest_schema_version = "1.1"

[plugin]
name = "threshold-deadman-checks"
version = "0.1.0"
description = "Threshold and deadman alerting checks."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"
