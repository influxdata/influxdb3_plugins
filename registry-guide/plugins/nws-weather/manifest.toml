manifest_schema_version = "1.1"

[plugin]
name = "nws-weather"
version = "0.1.0"
description = "Sample weather data from the National Weather Service."
triggers = ["process_scheduled_call"]

[dependencies]
database_version = ">=3.0.0"
