manifest_schema_version = "1.1"

[plugin]
name = "opcua"
version = "0.1.0"
description = "Collect data from OPC UA servers."
triggers = ["process_scheduled_call"]

[dependencies]
database_version = ">=3.0.0"
