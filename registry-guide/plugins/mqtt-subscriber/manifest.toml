manifest_schema_version = "1.1"

[plugin]
name = "mqtt-subscriber"
version = "0.1.0"
description = "Subscribe to MQTT topics and write messages to InfluxDB."
triggers = ["process_scheduled_call"]

[dependencies]
database_version = ">=3.0.0"
