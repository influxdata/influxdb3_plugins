manifest_schema_version = "1.1"

[plugin]
name = "system-metrics"
version = "0.2.0"
description = "Collect host system metrics on a schedule."
triggers = ["process_scheduled_call"]

[dependencies]
database_version = ">=3.0.0"
