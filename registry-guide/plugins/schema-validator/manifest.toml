manifest_schema_version = "1.1"

[plugin]
name = "schema-validator"
version = "0.1.0"
description = "Validate incoming writes against a schema."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"
