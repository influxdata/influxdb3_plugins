manifest_schema_version = "1.1"

[plugin]
name = "state-change"
version = "0.1.0"
description = "Detect state changes and emit alerts."
triggers = ["process_writes"]

[dependencies]
database_version = ">=3.0.0"
